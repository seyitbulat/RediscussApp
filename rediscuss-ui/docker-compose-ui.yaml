services:
  rediscuss-ui:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - 3000:3000
    stdin_open: true
    volumes:
      - .:/usr/src/app
      - /usr/src/app/node_modules
    restart: always
    